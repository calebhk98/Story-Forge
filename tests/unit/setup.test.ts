/**
 * @fileoverview Trivial test verifying the Vitest setup is functional.
 * Delete or replace once real unit tests exist.
 */

import { describe, it, expect } from 'vitest';

describe('test setup', () => {
  it('runs', () => {
    expect(true).toBe(true);
  });
});
